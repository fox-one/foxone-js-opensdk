import accountService from '../src/Account'
import expect from 'chai'

describe('test account service', function () {
  // it('test getLoginSmsCode api', function () {
  //   accountService.getLoginSmsCode().then(res => {
  //     expect(res).to.be.an(Object)
  //   })
  // })

  it('test', function () {
    expect(1).to.be.a(Number)
  })
})

